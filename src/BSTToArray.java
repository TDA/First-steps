import java.util.ArrayList;
import common.Node;
/**
 * Created by schandramouli on 9/19/17.
 */
public class BSTToArray {
    // Input is a BST formed from an array
    // Array was traversed from left to right
    // Objective is to find *all* possible arrays
    // which can result in this particular BST
    void BSTSequences(Node n)
    {
    }

    void printBSTSequence(ArrayList<Node> choices, ArrayList<Integer> combo) {

    }
}
